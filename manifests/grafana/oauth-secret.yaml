apiVersion: v1
kind: Secret
metadata:
  name: grafana-oauth
  namespace: monitoring
  labels:
    app: grafana
type: Opaque
stringData:
  # Replace these values with your Keycloak client credentials before applying
  client-id: "<KEYCLOAK_CLIENT_ID>"
  client-secret: "<KEYCLOAK_CLIENT_SECRET>"
